
FunctionA();
const buttonA = document.querySelector("#button_A");
const answerA = document.querySelector("#answer_A");


function FunctionA() {
    var dose = document.querySelector("#dose-départ-IECA-ARA").value;
    var rxd = document.querySelector("#ieca-ara-départ").value;
    var rxf = document.querySelector("#ieca-ara-final").value;


    // Captopril---------------------------------------------------------------------

        // Captopril vers Captopril
        if (rxd === "captopril" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Cizalapril
        if (rxd === "captopril" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Énalapril
        if (rxd === "captopril" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Fosinopril
        if (rxd === "captopril" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Lisinopril
        if (rxd === "captopril" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Périndopril
        if (rxd === "captopril" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Quinapril
        if (rxd === "captopril" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Ramipril
        if (rxd === "captopril" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Trandolapril
        if (rxd === "captopril" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Azilsartan
        if (rxd === "captopril" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Candésartan
        if (rxd === "captopril" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Irbésartan
        if (rxd === "captopril" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Losartan
        if (rxd === "captopril" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Olmésartan
        if (rxd === "captopril" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Telmisartan
        if (rxd === "captopril" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Captopril vers Valsartan
        if (rxd === "captopril" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Cizalapril---------------------------------------------------------------------

        // Cizalapril vers Captopril
        if (rxd === "cizalapril" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Cizalapril
        if (rxd === "cizalapril" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Énalapril
        if (rxd === "cizalapril" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Fosinopril
        if (rxd === "cizalapril" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Lisinopril
        if (rxd === "cizalapril" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Périndopril
        if (rxd === "cizalapril" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Quinapril
        if (rxd === "cizalapril" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Ramipril
        if (rxd === "cizalapril" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Trandolapril
        if (rxd === "cizalapril" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Azilsartan
        if (rxd === "cizalapril" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Candésartan
        if (rxd === "cizalapril" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Irbésartan
        if (rxd === "cizalapril" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Losartan
        if (rxd === "cizalapril" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Olmésartan
        if (rxd === "cizalapril" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Telmisartan
        if (rxd === "cizalapril" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Cizalapril vers Valsartan
        if (rxd === "cizalapril" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Énalapril---------------------------------------------------------------------

        // Énalapril vers Captopril
        if (rxd === "enalapril" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Cizalapril
        if (rxd === "enalapril" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Énalapril
        if (rxd === "enalapril" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Fosinopril
        if (rxd === "enalapril" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Lisinopril
        if (rxd === "enalapril" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Périndopril
        if (rxd === "enalapril" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Quinapril
        if (rxd === "enalapril" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Ramipril
        if (rxd === "enalapril" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Trandolapril
        if (rxd === "enalapril" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Azilsartan
        if (rxd === "enalapril" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Candésartan
        if (rxd === "enalapril" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Irbésartan
        if (rxd === "enalapril" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Losartan
        if (rxd === "enalapril" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Olmésartan
        if (rxd === "enalapril" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Telmisartan
        if (rxd === "enalapril" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Énalapril vers Valsartan
        if (rxd === "enalapril" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Fosinopril---------------------------------------------------------------------

        // Fosinopril vers Captopril
        if (rxd === "fosinopril" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Cizalapril
        if (rxd === "fosinopril" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Énalapril
        if (rxd === "fosinopril" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Fosinopril
        if (rxd === "fosinopril" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Lisinopril
        if (rxd === "fosinopril" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Périndopril
        if (rxd === "fosinopril" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Quinapril
        if (rxd === "fosinopril" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Ramipril
        if (rxd === "fosinopril" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Trandolapril
        if (rxd === "fosinopril" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Azilsartan
        if (rxd === "fosinopril" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Candésartan
        if (rxd === "fosinopril" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Irbésartan
        if (rxd === "fosinopril" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Losartan
        if (rxd === "fosinopril" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Olmésartan
        if (rxd === "fosinopril" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Telmisartan
        if (rxd === "fosinopril" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Fosinopril vers Valsartan
        if (rxd === "fosinopril" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Lisinopril---------------------------------------------------------------------

        // Lisinopril vers Captopril
        if (rxd === "lisinopril" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Cizalapril
        if (rxd === "lisinopril" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Énalapril
        if (rxd === "lisinopril" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Fosinopril
        if (rxd === "lisinopril" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Lisinopril
        if (rxd === "lisinopril" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Périndopril
        if (rxd === "lisinopril" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Quinapril
        if (rxd === "lisinopril" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Ramipril
        if (rxd === "lisinopril" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Trandolapril
        if (rxd === "lisinopril" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Azilsartan
        if (rxd === "lisinopril" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Candésartan
        if (rxd === "lisinopril" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Irbésartan
        if (rxd === "lisinopril" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Losartan
        if (rxd === "lisinopril" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Olmésartan
        if (rxd === "lisinopril" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Telmisartan
        if (rxd === "lisinopril" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Lisinopril vers Valsartan
        if (rxd === "lisinopril" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Périndopril---------------------------------------------------------------------

        // Périndopril vers Captopril
        if (rxd === "perindopril" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Cizalapril
        if (rxd === "perindopril" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Énalapril
        if (rxd === "perindopril" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Fosinopril
        if (rxd === "perindopril" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Lisinopril
        if (rxd === "perindopril" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Périndopril
        if (rxd === "perindopril" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Quinapril
        if (rxd === "perindopril" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Ramipril
        if (rxd === "perindopril" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Trandolapril
        if (rxd === "perindopril" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Azilsartan
        if (rxd === "perindopril" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Candésartan
        if (rxd === "perindopril" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Irbésartan
        if (rxd === "perindopril" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Losartan
        if (rxd === "perindopril" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Olmésartan
        if (rxd === "perindopril" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Telmisartan
        if (rxd === "perindopril" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Périndopril vers Valsartan
        if (rxd === "perindopril" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Quinapril---------------------------------------------------------------------

        // Quinapril vers Captopril
        if (rxd === "quinapril" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Cizalapril
        if (rxd === "quinapril" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Énalapril
        if (rxd === "quinapril" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Fosinopril
        if (rxd === "quinapril" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Lisinopril
        if (rxd === "quinapril" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Périndopril
        if (rxd === "quinapril" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Quinapril
        if (rxd === "quinapril" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Ramipril
        if (rxd === "quinapril" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Trandolapril
        if (rxd === "quinapril" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Azilsartan
        if (rxd === "quinapril" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Candésartan
        if (rxd === "quinapril" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Irbésartan
        if (rxd === "quinapril" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Losartan
        if (rxd === "quinapril" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Olmésartan
        if (rxd === "quinapril" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Telmisartan
        if (rxd === "quinapril" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Quinapril vers Valsartan
        if (rxd === "quinapril" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Ramipril---------------------------------------------------------------------

        // Ramipril vers Captopril
        if (rxd === "ramipril" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Cizalapril
        if (rxd === "ramipril" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Énalapril
        if (rxd === "ramipril" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Fosinopril
        if (rxd === "ramipril" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Lisinopril
        if (rxd === "ramipril" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Périndopril
        if (rxd === "ramipril" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Quinapril
        if (rxd === "ramipril" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Ramipril
        if (rxd === "ramipril" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Trandolapril
        if (rxd === "ramipril" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Azilsartan
        if (rxd === "ramipril" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Candésartan
        if (rxd === "ramipril" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Irbésartan
        if (rxd === "ramipril" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Losartan
        if (rxd === "ramipril" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Olmésartan
        if (rxd === "ramipril" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Telmisartan
        if (rxd === "ramipril" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Ramipril vers Valsartan
        if (rxd === "ramipril" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Trandolapril---------------------------------------------------------------------

        // Trandolapril vers Captopril
        if (rxd === "trandolapril" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Cizalapril
        if (rxd === "trandolapril" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Énalapril
        if (rxd === "trandolapril" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Fosinopril
        if (rxd === "trandolapril" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Lisinopril
        if (rxd === "trandolapril" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Périndopril
        if (rxd === "trandolapril" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Quinapril
        if (rxd === "trandolapril" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Ramipril
        if (rxd === "trandolapril" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Trandolapril
        if (rxd === "trandolapril" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Azilsartan
        if (rxd === "trandolapril" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Candésartan
        if (rxd === "trandolapril" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Irbésartan
        if (rxd === "trandolapril" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Losartan
        if (rxd === "trandolapril" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Olmésartan
        if (rxd === "trandolapril" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Telmisartan
        if (rxd === "trandolapril" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Trandolapril vers Valsartan
        if (rxd === "trandolapril" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Azilsartan---------------------------------------------------------------------

        // Azilsartan vers Captopril
        if (rxd === "azilsartan" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Cizalapril
        if (rxd === "azilsartan" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Énalapril
        if (rxd === "azilsartan" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Fosinopril
        if (rxd === "azilsartan" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Lisinopril
        if (rxd === "azilsartan" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Périndopril
        if (rxd === "azilsartan" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Quinapril
        if (rxd === "azilsartan" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Ramipril
        if (rxd === "azilsartan" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Trandolapril
        if (rxd === "azilsartan" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Azilsartan
        if (rxd === "azilsartan" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Candésartan
        if (rxd === "azilsartan" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Irbésartan
        if (rxd === "azilsartan" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Losartan
        if (rxd === "azilsartan" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Olmésartan
        if (rxd === "azilsartan" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Telmisartan
        if (rxd === "azilsartan" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Azilsartan vers Valsartan
        if (rxd === "azilsartan" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Candésartan---------------------------------------------------------------------

        // Candésartan vers Captopril
        if (rxd === "candésartan" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Cizalapril
        if (rxd === "candésartan" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Énalapril
        if (rxd === "candésartan" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Fosinopril
        if (rxd === "candésartan" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Lisinopril
        if (rxd === "candésartan" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Périndopril
        if (rxd === "candésartan" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Quinapril
        if (rxd === "candésartan" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Ramipril
        if (rxd === "candésartan" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Trandolapril
        if (rxd === "candésartan" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Azilsartan
        if (rxd === "candésartan" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Candésartan
        if (rxd === "candésartan" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Irbésartan
        if (rxd === "candésartan" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Losartan
        if (rxd === "candésartan" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Olmésartan
        if (rxd === "candésartan" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Telmisartan
        if (rxd === "candésartan" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Candésartan vers Valsartan
        if (rxd === "candésartan" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Irbésartan---------------------------------------------------------------------

        // Irbésartan vers Captopril
        if (rxd === "irbesartan" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Cizalapril
        if (rxd === "irbesartan" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Énalapril
        if (rxd === "irbesartan" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Fosinopril
        if (rxd === "irbesartan" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Lisinopril
        if (rxd === "irbesartan" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Périndopril
        if (rxd === "irbesartan" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Quinapril
        if (rxd === "irbesartan" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Ramipril
        if (rxd === "irbesartan" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Trandolapril
        if (rxd === "irbesartan" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Azilsartan
        if (rxd === "irbesartan" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Candésartan
        if (rxd === "irbesartan" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Irbésartan
        if (rxd === "irbesartan" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Losartan
        if (rxd === "irbesartan" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Olmésartan
        if (rxd === "irbesartan" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Telmisartan
        if (rxd === "irbesartan" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Irbésartan vers Valsartan
        if (rxd === "irbesartan" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Losartan---------------------------------------------------------------------

        // Losartan vers Captopril
        if (rxd === "losartan" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Cizalapril
        if (rxd === "losartan" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Énalapril
        if (rxd === "losartan" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Fosinopril
        if (rxd === "losartan" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Lisinopril
        if (rxd === "losartan" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Périndopril
        if (rxd === "losartan" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Quinapril
        if (rxd === "losartan" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Ramipril
        if (rxd === "losartan" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Trandolapril
        if (rxd === "losartan" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Azilsartan
        if (rxd === "losartan" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Candésartan
        if (rxd === "losartan" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Irbésartan
        if (rxd === "losartan" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Losartan
        if (rxd === "losartan" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Olmésartan
        if (rxd === "losartan" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Telmisartan
        if (rxd === "losartan" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Losartan vers Valsartan
        if (rxd === "losartan" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Olmésartan---------------------------------------------------------------------

        // Olmésartan vers Captopril
        if (rxd === "olmésartan" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Cizalapril
        if (rxd === "olmésartan" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Énalapril
        if (rxd === "olmésartan" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Fosinopril
        if (rxd === "olmésartan" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Lisinopril
        if (rxd === "olmésartan" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Périndopril
        if (rxd === "olmésartan" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Quinapril
        if (rxd === "olmésartan" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Ramipril
        if (rxd === "olmésartan" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Trandolapril
        if (rxd === "olmésartan" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Azilsartan
        if (rxd === "olmésartan" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Candésartan
        if (rxd === "olmésartan" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Irbésartan
        if (rxd === "olmésartan" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Losartan
        if (rxd === "olmésartan" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Olmésartan
        if (rxd === "olmésartan" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Telmisartan
        if (rxd === "olmésartan" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Olmésartan vers Valsartan
        if (rxd === "olmésartan" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Telmisartan---------------------------------------------------------------------

        // Telmisartan vers Captopril
        if (rxd === "telmisartan" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Cizalapril
        if (rxd === "telmisartan" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Énalapril
        if (rxd === "telmisartan" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Fosinopril
        if (rxd === "telmisartan" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Lisinopril
        if (rxd === "telmisartan" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Périndopril
        if (rxd === "telmisartan" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Quinapril
        if (rxd === "telmisartan" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Ramipril
        if (rxd === "telmisartan" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Trandolapril
        if (rxd === "telmisartan" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Azilsartan
        if (rxd === "telmisartan" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Candésartan
        if (rxd === "telmisartan" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Irbésartan
        if (rxd === "telmisartan" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Losartan
        if (rxd === "telmisartan" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Olmésartan
        if (rxd === "telmisartan" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Telmisartan
        if (rxd === "telmisartan" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Telmisartan vers Valsartan
        if (rxd === "telmisartan" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }

    // Valsartan---------------------------------------------------------------------

        // Valsartan vers Captopril
        if (rxd === "valsartan" && rxf === "captopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Cizalapril
        if (rxd === "valsartan" && rxf === "cizalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Énalapril
        if (rxd === "valsartan" && rxf === "enalapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Fosinopril
        if (rxd === "valsartan" && rxf === "fosinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Lisinopril
        if (rxd === "valsartan" && rxf === "lisinopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Périndopril
        if (rxd === "valsartan" && rxf === "perindopril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Quinapril
        if (rxd === "valsartan" && rxf === "quinapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Ramipril
        if (rxd === "valsartan" && rxf === "ramipril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Trandolapril
        if (rxd === "valsartan" && rxf === "trandolapril") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Azilsartan
        if (rxd === "valsartan" && rxf === "azilsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Candésartan
        if (rxd === "valsartan" && rxf === "candésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Irbésartan
        if (rxd === "valsartan" && rxf === "irbesartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Losartan
        if (rxd === "valsartan" && rxf === "losartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Olmésartan
        if (rxd === "valsartan" && rxf === "olmésartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Telmisartan
        if (rxd === "valsartan" && rxf === "telmisartan") {
            malcom = (Math.round(dose * 10) / 10);
        }
        // Valsartan vers Valsartan
        if (rxd === "valsartan" && rxf === "valsartan") {
            malcom = (Math.round(dose * 10) / 10);
        }



        if (rxd === "captopril") {
            rxd1 = "Captopril";
        }
        if (rxd === "cizalapril") {
            rxd1 = "Cizalapril";
        }
        if (rxd === "enalapril") {
            rxd1 = "Énalapril";
        }
        if (rxd === "fosinopril") {
            rxd1 = "Fosinopril";
        }
        if (rxd === "lisinopril") {
            rxd1 = "Lisinopril";
        }
        if (rxd === "perindopril") {
            rxd1 = "Périndopril";
        }
        if (rxd === "quinapril") {
            rxd1 = "Quinapril";
        }
        if (rxd === "ramipril") {
            rxd1 = "Ramipril";
        }
        if (rxd === "trandolapril") {
            rxd1 = "Trandolapril";
        }
        if (rxd === "azilsartan") {
            rxd1 = "Azilsartan";
        }
        if (rxd === "candésartan") {
            rxd1 = "Candésartan";
        }
        if (rxd === "irbesartan") {
            rxd1 = "Irbésartan";
        }
        if (rxd === "losartan") {
            rxd1 = "Losartan";
        }
        if (rxd === "olmésartan") {
            rxd1 = "Olmésartan";
        }
        if (rxd === "telmisartan") {
            rxd1 = "Telmisartan";
        }
        if (rxd === "valsartan") {
            rxd1 = "Valsartan";
        }






        if (rxf === "captopril") {
            rxf1 = "captopril";
        }
        if (rxf === "cizalapril") {
            rxf1 = "cizalapril";
        }
        if (rxf === "enalapril") {
            rxf1 = "énalapril";
        }
        if (rxf === "fosinopril") {
            rxf1 = "fosinopril";
        }
        if (rxf === "lisinopril") {
            rxf1 = "lisinopril";
        }
        if (rxf === "perindopril") {
            rxf1 = "périndopril";
        }
        if (rxf === "quinapril") {
            rxf1 = "quinapril";
        }
        if (rxf === "ramipril") {
            rxf1 = "ramipril";
        }
        if (rxf === "trandolapril") {
            rxf1 = "trandolapril";
        }
        if (rxf === "azilsartan") {
            rxf1 = "azilsartan";
        }
        if (rxf === "candésartan") {
            rxf1 = "candésartan";
        }
        if (rxf === "irbesartan") {
            rxf1 = "irbésartan";
        }
        if (rxf === "losartan") {
            rxf1 = "losartan";
        }
        if (rxf === "olmésartan") {
            rxf1 = "olmésartan";
        }
        if (rxf === "telmisartan") {
            rxf1 = "telmisartan";
        }
        if (rxf === "valsartan") {
            rxf1 = "valsartan";
        }


        let yolo1 = rxd1 + " " + dose + "mg équivaut à " + rxf1 + " " + malcom + "mg";
        document.getElementById("answer_A").textContent = yolo1;

}
